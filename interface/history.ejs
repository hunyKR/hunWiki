<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      header {
        position: relative;
        background-color: skyblue;
      }

      #go-main {
        font-size: 2em;
        color: black;
        margin-left: 30px;
      }

      .search {
        position: absolute;
        right: 0px;
        bottom: 10px;
      }

      #search-input {
        margin-right: 50px;
      }

      .content {
        white-space: pre-line;
      }
    </style>
    <title><%= title %> - Wiki</title>
  </head>
  <body>
    <header>
      <a href="/" id="go-main">Wiki</a>
      <input type="text" class="search" id="search-input" />
      <input type="button" value="검색" class="search" onclick="search()" />
    </header>

    <a href="/w/<%= title %>" style="display: block">이전</a>
    <% data.forEach((v)=>{ %>
    <div>
      <strong>r<%= v.rev %> </strong><span><%= v.date %> </span
      ><span style="color: blue"><%= v.ip %> </span>
      <span>[<%= v.summary %>] </span><i>(<%= v.method %>) </i>
      <a href="/w/legacy/<%= title %>?q=<%= v.rev %>">보기</a>
      <a
        href="javascript:location.href = '/s/edit/rec-rev?title=<%= title %>&q=<%= v.rev %>&sum=' + prompt('편집 요약을 입력하세요.')"
        >이 리비전으로 되돌리기</a
      >
    </div>
    <% }) %>
  </body>
  <script src="
  https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.min.js
  "></script>
  <script>
    const searchInput = $("#search-input");
    function search() {
      location.href = "/s/search?q=" + encodeURIComponent(searchInput.val());
    }
  </script>
</html>
